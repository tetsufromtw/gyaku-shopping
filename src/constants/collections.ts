// Firestore Collection Names
export const COLLECTIONS = {
  USERS: 'users',
  POSTS: 'posts',
  COMMENTS: 'comments',
} as const;

export const STORAGE_PATHS = {
  POST_IMAGES: 'post-images',
} as const;
